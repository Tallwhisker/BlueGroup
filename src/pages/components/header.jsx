import { Outlet, Link } from "react-router-dom";

export default function Header() {
    return (
        <header>
            <nav>
                <h1>Navbar</h1>
            </nav>
        </header>
    );
}